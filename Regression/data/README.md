# Data Directory

This GitHub package includes only small metadata and calibration files needed to document the pipeline.

Do not commit raw microscopy images, cached tensors, trained models, large prediction CSVs, or generated paper packages to GitHub. Store those artifacts in Zenodo and link the DOI from the project README.
